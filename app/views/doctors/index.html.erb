<h1>Seattle Grace Hospital Doctors: </h1>

<% @doctors.each do |dr| %>
<ul>
  <strong><p>Doctor Profile: </p></strong>
  <ul>
    <li><%= link_to dr.name, doctor_path(dr) %></li>
    <li><%= pluralize(dr.patient_count, "patient") %></li>
  </ul>
</ul>
<% end %>
